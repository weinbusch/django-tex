language: python
python:
  - "3.6"
sudo: enabled
before_install:
  - "sudo apt-get --assume-yes --no-install-recommends install texlive-latex-base"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - python manage.py test
